# Personal state: WaniKani pull, generated stories. Gitignored so a clone
# starts clean and nobody inherits someone else's progress.
me/

# Pre-rebuild tutoring apparatus. Kept locally until confirmed unwanted;
# tracked files are also recoverable from the archive/tutor-v1 tag.
me.archive-*/

.DS_Store
